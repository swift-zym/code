use db
create table hack
select * from `user`.`password` join `hack`.`password` 
drop table user
select * from `data`.`VM`.`location` join `hack`.`ip`
drop table data
selcct * from `new` join hack.`files`
drop table new 
drop database
use `hack`.`sql_install`
